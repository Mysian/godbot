// battle-system/context.js
module.exports = {
  applyEffects(user, enemy, context) {
    // 도트, 기절, 보호막 등 상태효과 처리
    // 예시: context.effects[user.id] = [{type: 'dot', ...}, ...]
    // 이 함수에 로직 추가 가능!
  }
};
